export interface IProduct{ //Interface is created to set some basic rules for properties and methods using class
    productId: number;
    productName: string;
    productCode: string;
    releaseDate: string;
    description: string;
    price: number;
    starrating: number;
    imageUrl: string;
}